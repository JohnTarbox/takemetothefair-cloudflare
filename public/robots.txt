# robots.txt for Meet Me at the Fair
User-agent: *
Allow: /

# Disallow admin and private areas
Disallow: /admin/
Disallow: /api/
Disallow: /dashboard/
# NOTE (OPE-87): the singular /vendor/ and /promoter/ prefixes are the private
# user portals, but they are deliberately NOT disallowed here. Blocking them made
# stray singular links to real public records (e.g. a blog typo /vendor/<slug>)
# get indexed URL-only ("Indexed, though blocked by robots.txt"). Instead the
# portal pages are protected at the app layer (auth gate + page-level noindex),
# and middleware 301s any singular /vendor/<slug> or /promoter/<slug> to the
# plural public detail page. Public listings live at /vendors and /promoters.

# Explicit allow for AI crawlers — newer agents look for per-bot rules as a
# positive signal even when the wildcard above already permits them.
User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: OAI-SearchBot
Allow: /

User-agent: Applebot-Extended
Allow: /

User-agent: CCBot
Allow: /

# Sitemap
Sitemap: https://meetmeatthefair.com/sitemap.xml
